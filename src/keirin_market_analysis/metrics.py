from __future__ import annotations


def compute_c(*args, **kwargs):
    raise NotImplementedError(
        "C metric is intentionally undefined until its exact agreed formula is fixed."
    )


def compute_h(*args, **kwargs):
    raise NotImplementedError(
        "H metric is intentionally undefined until its exact agreed formula is fixed."
    )

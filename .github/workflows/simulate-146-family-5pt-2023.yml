name: Simulate 146 family 5pt 2023

on:
  push:
    paths:
      - 'src/keirin_market_analysis/simulate_146_family_5pt_2023.py'
      - '.github/workflows/simulate-146-family-5pt-2023.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python src/keirin_market_analysis/simulate_146_family_5pt_2023.py
      - name: Commit audit output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/audits/market_support_146_family_5pt_2023.json
          git diff --cached --quiet || git commit -m "Add 146 family 5-point 2023 simulation output"
          git push

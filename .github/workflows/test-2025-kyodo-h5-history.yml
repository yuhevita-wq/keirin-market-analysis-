name: Test 2025 Kyodo H5 History Relation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/keirin_shogi_kyodo_2025_history_h5_test.py'
      - '.github/workflows/test-2025-kyodo-h5-history.yml'

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e .
      - run: python scripts/keirin_shogi_kyodo_2025_history_h5_test.py
      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/keirin_shogi/kyodo_2025_history_h5_test.json
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "research: save 2025 Kyodo H5 history relation result"
            git pull --rebase origin main
            git push
          fi

name: Analyze middle scenario other ticket payouts 2023-2024

on:
  push:
    paths:
      - 'src/keirin_market_analysis/analyze_middle_other_ticket_payouts_2023_2024.py'
      - '.github/workflows/analyze-middle-other-ticket-payouts-2023-2024.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -e .
      - run: python -m keirin_market_analysis.analyze_middle_other_ticket_payouts_2023_2024
      - name: Validate scope
        run: |
          python - <<'PY'
          import json
          for p in ['data/audits/middle_other_ticket_payouts_2023_2024.json','data/audits/middle_other_ticket_payouts_2023_2024_compact.json']:
              d=json.load(open(p,encoding='utf-8'))
              assert d['years_read']==[2023,2024]
              assert d['evaluation_year_2025_used'] is False
              assert d['evaluation_year_2026_used'] is False
          PY
      - name: Commit result
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/audits/middle_other_ticket_payouts_2023_2024.json data/audits/middle_other_ticket_payouts_2023_2024_compact.json
          git commit -m 'Add compact middle scenario other-ticket payout audit' || exit 0
          git push
